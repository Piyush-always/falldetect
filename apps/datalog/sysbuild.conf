#
# Builds MCUboot as a child image for BLE OTA. Identical recipe to
# apps/blink/sysbuild.conf and the root firmware's sysbuild.conf - same board,
# same partition layout, proved out first in apps/blink.
#
SB_CONFIG_BOOTLOADER_MCUBOOT=y
