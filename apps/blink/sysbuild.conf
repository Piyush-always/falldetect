#
# Builds MCUboot as a child image so blink can be updated over BLE (OTA)
# instead of only via UF2 drag-and-drop. See apps/blink/pm_static.yml for the
# partition layout this requires.
#
SB_CONFIG_BOOTLOADER_MCUBOOT=y
